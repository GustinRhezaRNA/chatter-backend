version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 25.2.1
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
